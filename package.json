{
  "name": "pricefunc",
  "version": "0.1.0",
  "private": true,
  "packageManager": "npm@11.12.1",
  "description": "Estimador de Pontos de Função — Precificação inteligente para software houses",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run setup && turbo run dev",
    "setup": "npm run prisma:generate --workspace=@pricefunc/api && npm run prisma:migrate:deploy --workspace=@pricefunc/api && npm run prisma:seed --workspace=@pricefunc/api",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev:api": "npm run setup && npm run dev --workspace=apps/api",
    "dev:web": "npm run dev --workspace=apps/web"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "vitest": "^3.2.7"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
